#include <sys/types.h>
#include <sys/wait.h>
#include <unistd.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <iostream>
using namespace std;
int main (int argc, char*argv[])
{ 
	if (argc>1
	printf("For being a bad boy/girl and not changing your default passwords I will have to punish you >:D \n1) You can only use default ls command without any parameters\n");
	printf("\n2)What is the Swiss Army Knife Of Networking?(pls lower case, I got lazy at this point)");
	string test;
	cin>>test;
	if(test.compare("netcat")!=0)
	cout<<"Just google it";
	return 0;
	}
        char name[200];
	strcpy(name, "/bin/ls ");
	system(name);
}


